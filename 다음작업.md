# 다음 작업 (상세 계획)

> 현재까지: RAG 스택(weaviate·law_embedding·law_agent) 도커화 + compose 통합 테스트 통과 + 커밋/push 완료.
> 남은 3개를 상세히 잡음. 배경/결정은 `CLAUDE.md §4-B/§4-C` 참고.

---

## 0. 아키텍처 결정 (폐쇄망/모드) — 먼저 읽기

문제의 본질 = git/db 선택이 아니라 **"변경분을 어떻게 만들고(생산) / 나르고(전송) / 적용하냐(소비)"**가 섞인 것.
→ **3층 분리 + change-set 계약**으로 git/db·폐쇄망을 나중에 갈아끼워도 색인 코드 불변으로 만든다.

```
① 생산자(모드별)      ② 전송(토폴로지)                     ③ 소비자(모드·전송 무관)
git diff ─┐                                               ┌ law_id 단위 삭제 + 재임베딩
db query ─┴▶ change-set ─▶ [내부:직접호출 / 폐쇄망:아티팩트반입] ─┴▶ Weaviate 갱신
            {changed:[{law_id, payload}], deleted:[law_id]}
```

- **change-set = 표준 단위**: `{바뀐 law_id+payload, 삭제된 law_id}`. git이든 db든 **같은 change-set** 생성 → git/db 지금 안 정해도 됨.
- **소비자(증분 색인기)는 모드·전송을 모름** → change-set만 받아 적용. 폐쇄망/내부 둘 다 색인 코드 동일.

**토폴로지 매핑**
| | 내부/일반 | 폐쇄망 (DMZ↔내부) |
|---|---|---|
| 수집 | Temporal 한 판에서 collect | **DMZ**에서 collect (인터넷 O) |
| 전달 | Temporal이 소비자 **직접 호출**(activity) | change-set을 **아티팩트 반입**(git bundle \| JSON+minio URL) → 내부 소비자 적용 |
| 초기 | 전체 색인 | **벌크 스냅샷 1회**(data 전체 + Weaviate 백업) 반입 |

- 내부는 오버킬 불필요 → Temporal `collect → 증분색인 activity`. 폐쇄망은 **같은 소비자**를 change-set 먹는 잡으로 트리거만 교체.
- **파일(DB모드)**: payload 파일참조를 **minio/filestorage URL**로 → 폐쇄망 내부 minio 가 해소. change-set엔 URL만 실림(바이너리 반입 최소화).
- **초기 반입은 벌크**(증분 아님) — 첫 반입만 전체 스냅샷, 이후만 change-set.

**결정**: 증분 색인기를 **change-set 계약**으로 구현(항목1). 생산자(git-diff/db-query)와 전송(직접/아티팩트)은 그 위에 얇게. 폐쇄망 세부(git 허용 여부·minio)는 확정 후 생산자·전송만 붙임.

**확정 (2026-07-28)**
- 수집기는 **DB 항상 보유(STORAGE_MODE=both)** 로 간다 → both 라 git 파일도 생겨 현재 파일기반 임베딩·git_history 그대로 동작. "DB-only(git 없음)" 대응은 나중 과제.
- 증분/임베딩은 **먼저 CLI 잡**으로 구현(change-set 소비). 내부(Temporal)용은 그 함수를 **activity 로 감싸기만**. 지금 worker 등록 불필요.
- 현재 없음: 임베딩쪽 **DB 소스 리더**·**증분**(파일 전체색인만), **git_history 는 git 전용**(DB-only 이력조회 없음).
- **전처리 위치 안 바꿈**: 지금 law_embedding 쪽 전처리(목업) 그대로. 이미 수집 끝난 데이터를 현재 방식대로 임베딩. "전처리를 DMZ로"는 폐쇄망 배포 시점의 배치 결정일 뿐 지금 코드 이동 아님(§아키텍처.md). 내부 전처리 유지 시 hwp/pdf 바이너리가 경계 넘어야 함 — 그게 싫을 때만 그때 DMZ로.

**지금 할 것 (로컬·수집완료 데이터 기준) — 축소 스코프**
1. **증분 색인기**(항목1의 change-set 소비 코어): `index-changed` CLI = `git diff <watermark>..HEAD` → 바뀐 law_id → **law_id 단위 삭제+재임베딩** → watermark 갱신. ⚠️embedding 이미지에 git 추가.
2. **VM 이전 도구**(항목2): Weaviate 백업/복원 + 런북.

**지금 보류**: 전처리 DMZ 이동 / DB 소스 리더·db 생산자 / Kafka 수신에이전트(로컬=디렉토리) / 판례·복지로·서민금융 정규화기 / 수집기 도커화(항목3).

---

## 1. 증분 색인 — "변경분만 적재" (law_embedding)

**목표**: **바뀐 법만** 재임베딩. 핵심 = **change-set(§0)을 먹는 모드·전송 무관 소비자.** 생산자는 `INDEX_SOURCE=git|db`.

**핵심 원리 (반드시 지킬 것)**
- 법이 개정되면 `version_uid`(=law_id:MST:시행일)가 바뀜 → chunk_id/UUID(결정적)도 바뀜 → **단순 upsert 로는 옛 버전 청크가 orphan 으로 남음.**
- 그래서 문서 단위로 **① 그 law_id(또는 git_path) 객체 전부 삭제 → ② 현재 버전 재임베딩·삽입** ("싹 갈아서"). git/db 모드 공통.

**작업 항목**
- [ ] `config.py`: `INDEX_SOURCE`(git|db, 기본 files-all=전체) 추가.
- [ ] `weaviate_store.py`: `delete_by_law_id(law_id)` 추가 — `collection.data.delete_many(where=Filter.by_property("law_id").equal(law_id))`. (또는 git_path 기준)
- [ ] `pipeline.py`: `index_changed(...)`:
  - **git 모드**: `git -C <repo> diff --name-only <watermark_commit>..HEAD -- '*.json'` → 변경 JSON → 각 파일의 law_id 추출 → 삭제+색인.
  - **db 모드**: lawdb 에서 `updated_at > watermark` 인 document_version 조회 → law_id 목록 → 각 현재 payload 를 **DB에서 읽어** 삭제+색인.
- [ ] **watermark 저장**: git=마지막 색인 commit SHA / db=마지막 색인 timestamp.
  - 저장 위치 결정 필요: (a) 마운트 볼륨의 `.index_state.json` (b) Weaviate 메타 객체(별도 class `IndexState`) (c) weaviate 내 max(git_commit) 조회. → **(a) 상태파일 추천** (단순, 모드무관).
- [ ] **DB 소스 리더 (db 모드 전제, 신규)**: 지금 law_embedding 은 **파일만 읽음**. lawdb 접속(SQLAlchemy/psycopg) + document_version.payload 조회 코드 추가. → law_embedding 에 DB 의존성·접속 env(`LAWDB_URL` 등) 추가됨.
- [ ] `cli.py`: `index-changed` 서브커맨드 (또는 `index --source git|db --since <commit|ts>`).
- [ ] ⚠️ **law_embedding 이미지에 `git` 바이너리 없음** → git 모드 증분 쓰려면 Dockerfile 에 `apt-get install git` + `safe.directory` 추가 (agent 처럼). data repo 를 `.git` 포함해 마운트.
- [ ] (선택) content_hash 스킵: 바뀐 문서 안에서도 안 바뀐 조문은 embed 스킵 (이미 content_hash 적재돼 있음). 위 삭제후재적재 위에 얹는 최적화.

**완료 기준**: 법 1건 JSON(또는 DB row) 바꾸고 `index-changed` → 그 법 객체만 갱신, 옛 버전 청크 사라짐, 나머지 그대로. 전체 count 는 그 법만큼만 변동.

---

## 2. VM 이전 도구 — "폭 떠서 옮기기"

**목표**: 로컬 상태를 VM 으로. **Weaviate = 백업/복원 API**, 원천데이터 = 모드별(git pull | pg_dump).

**작업 항목**
- [ ] `docker-compose.rag.yml` weaviate 에 백업 모듈 켜기:
  - `ENABLE_MODULES=backup-filesystem`, `BACKUP_FILESYSTEM_PATH=/backups`
  - 바인드 `./weaviate_backups:/backups` (호스트에서 바로 tar/scp 가능하게)
- [ ] `scripts/weaviate_backup.sh <id>`: `curl -X POST .../v1/backups/filesystem -d '{"id":"<id>","include":["LegalProvisionIndex"]}'` → 상태 SUCCESS 까지 폴링.
- [ ] `scripts/weaviate_restore.sh <id>`: `curl -X POST .../v1/backups/filesystem/<id>/restore` → 폴링.
- [ ] 원천데이터 이전 (모드별):
  - **git**: VM 에서 `git clone/pull`(이미 GitHub 에 있음) — 별도 번들 불필요.
  - **db**: `pg_dump lawdb` → scp → VM `pg_restore`.
- [ ] `docs/migrate.md` 런북: 로컬 backup → scp `weaviate_backups/<id>` → VM restore, 모드별 순서.

**Gotcha**
- 백업/복원 API 는 **아키텍처 이식 가능**(Mac arm64 → x86 VM OK). **Weaviate 이미지 버전은 양쪽 동일**.
- **이미지 아키텍처**: 로컬은 arm64, VM 은 보통 x86_64 → 로컬 arm64 이미지는 VM 에서 안 돎. **VM 용은 `--platform linux/amd64` 로 빌드하거나 VM 에서 rebuild / 레지스트리 push**.

**완료 기준**: 로컬 backup → 빈 weaviate 에 restore → count·검색 결과 동일.

---

## 3. 수집기 스택 도커화 (temporal_law) — 마지막, 제일 무거움

**현재**: `temporal_law/docker-compose.yml` = temporal-db·temporal·temporal-ui·lawdb (인프라만). **worker 는 host 에서 `uv run python -m pipeline.worker` 로 돎(미도커화)**. worker Dockerfile 없음.

**작업 항목**
- [ ] **worker Dockerfile (난점)**: python+uv 베이스 + **headless Chromium + 한글 폰트**(render.py 가 정관/공공기관 렌더에 Chrome 사용). `chromium`, `fonts-nanum*` 설치, `--no-sandbox`, `/dev/shm` 크기 고려.
- [ ] compose 에 `worker` 서비스: temporal·lawdb 접속. env: `OC 키(시크릿)`, `STORAGE_MODE`, `GIT_EXPORT_*`, `LAW_ENABLED/ADMRUL_ENABLED`, `INCLUDE_LIST`.
- [ ] **git push 자격증명**: worker 가 `data/` 를 원격에 push → SSH 배포키 or PAT 를 **시크릿 마운트**(이미지에 굽지 말 것) + `git config safe.directory`. data repo 를 push 권한으로 클론해 볼륨 마운트.
- [ ] **스케줄**: 스택 up 후 `python -m pipeline.starter schedule` 1회 (또는 init 잡) → Temporal cron 이 매일 sync 발화. worker 는 상주(`restart: always`).
- [ ] **모드/영속화**: `STORAGE_MODE=both`(기본 계획) → lawdb 볼륨 영속화 + VM 최초 1회 **pg_dump/restore 로 DB 이관**(§2). git 모드면 `_manifest.json` 만으로 sync 됨(DB 이관 불필요).
- [ ] OC 키(`genosAPItestKey`) **절대 이미지/커밋에 넣지 말 것** (env/시크릿).

**Gotcha**
- Chrome 컨테이너화가 핵심 난관(deps·폰트·sandbox). 안 되면 `pi/public` 렌더 관계추출만 영향(고시/학칙은 무관).
- 리소스: Temporal+Chrome+postgres 무거움 → VM 사이징 넉넉히.

**완료 기준**: `compose up` → worker 가 Temporal 에 등록(UI 확인) → discover/backfill 1회 성공 → sync 스케줄 발화 → 데이터가 lawdb(+git 활성 시 data repo) 에 적재.

---

## 배포 배선 (위 3개 엮기)
- **매일 sync**: 수집기=Temporal 스케줄(항목3). RAG 재색인=cron `git pull(또는 DB) → index-changed`(항목1). Temporal 로 통합은 나중(수집기 Temporal 재사용).
- **두 스택 연결**: `data/` git repo (수집 push / RAG pull) 또는 both 모드 공유볼륨.
- **VM 이미지**: amd64 빌드 or 레지스트리 push (§2 Gotcha).

## 순서 제안
1) 증분 색인(항목1) — 당장 유용, RAG 스택만 건드림.
2) VM 이전 도구(항목2) — compose 소폭 + 스크립트.
3) 수집기 도커화(항목3) — 제일 무겁고 Chrome 난관, 마지막.
