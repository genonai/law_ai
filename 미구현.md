# 미구현 목록 — 아직 안 된 것 (개발 필요 시 참고)

> 배포 구성([todo.md](todo.md)) 중 **아직 구현되지 않은 부분**만 모음. 지금은 다 보류/목업.

---

## 1. DB dump 기반 초기적재 (보류)

**무엇**: 수집기 DB(Postgres)를 dump/restore 로 옮긴 뒤, 임베딩기가 **DB에서 직접 읽어 초기 임베딩**.

**왜 안 됨**:
- 임베딩기에 **Postgres 리더가 없다** — 파일(JSON)·package(JSONL)만 읽는다.
- `file_asset` 은 법제처 API URL 만 있고 **실제 저장 위치(MINIO 주소)가 비어 있다**.

**하려면 필요한 것(전부 딸려 옴)**:
- 수집기·임베딩기에 **MINIO 설치**.
- 수집 시 **`file_asset` 에 저장주소 채우기** 로직.
- 임베딩기 **DB 초기 임베딩** 로직(document_version.payload 읽어 매핑·적재).
- 증분 처리 시 **데이터 유지 옵션**(원본 보존 쪽).
- **원본 보존(보존 STEP)도 DB 개발이 딸려 온다** — 그래서 이 둘은 세트로 미룸.

**더 싼 대안(개발 시 이걸 먼저 검토)**:
- DB 리더/MINIO 를 새로 만들지 말고 **DB에서 full package 를 emit → 기존 소비 경로로 적재**.
  지금 코드 그대로 재사용. 초기 1회만 full mode. MINIO/file_asset 없이도 됨.

**상태**: 보류(지금 안 함).

---

## 2. API push 전송 (목업만 예정)

**무엇**: package 를 폴더/MINIO 가 아니라 **HTTP push(POST 등)** 로 외부 엔드포인트에 전송.

**현재**: 전송은 `folder` / `minio` / 같은클러스터 `INDEX_TASK_QUEUE 트리거` / `none` 만 됨.

**계획**: 확정 안 된 전송 방식들 대비해 **sink 인터페이스만 확장성 있게** 두고, `api` 는 **목업(스텁)** 으로만
만들어 둔다(실제 엔드포인트 계약이 정해지면 채운다).

**상태**: 목업만 만들 예정.

---

## 3. DB 기반 원본 보존 (보류 — 1번과 세트)

**무엇**: 소비자 원본 보존을 폴더(RepoMirror)/flat(OriginalStore)이 아니라 **DB/MINIO** 로.

**왜 보류**: 1번(DB 초기적재)의 MINIO·file_asset 개발이 선행돼야 함 → 같이 미룸.

**현재 되는 보존**: `PACKAGE_MIRROR_REPO`(레포 레이아웃) · `PACKAGE_STORE_ORIGINAL`(flat).

**상태**: 보류.

---

## 참고 — 이건 "미구현"이 아니라 "리팩터 예정"(todo.md G)

옵션 통합(manifest 흡수 · payload_source/소비자 preprocess 자동 유도 · stream edge화 · env STEP정렬 ·
DEPLOY_PROFILE)은 **기능이 없는 게 아니라 정리 작업** — 미구현 목록이 아니라 [todo.md](todo.md) 구현 순서에 있음.
</content>
