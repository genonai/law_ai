[project]
name = "provision-viewer-backend"
version = "0.1.0"
description = "provision_id로 조문을 조회하는 뷰어의 read-only 백엔드 (law_data/admrul_data GitHub 저장소를 clone 없이 직접 읽는다)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.138.2",
    "uvicorn[standard]>=0.49.0",
    "httpx>=0.28",
]

[tool.uv]
package = false
