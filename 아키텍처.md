# 외부연동 → 벡터DB 아키텍처 (폐쇄망/로컬 공통)

> 요구사항: 4개 공개 API(①법제처 현행법령 ②복지로 ③서민금융진흥원 ④대법원 판례)를 RAG 코퍼스로.
> 흐름: **외부망(API) → DMZ(호출 완결·정제·Diff·HWP/PDF 텍스트추출) → 망연계(MCCUBE, 파일 단방향) → 내부망(수신→Kafka→온프레 임베딩→VectorDB Upsert→RAG+온프레 LLM)**.
> 로컬에서도 동일 파이프라인이 "접힌 형태"로 돌아야 함.

## 0. 핵심 원칙 — 경계 계약은 "파일 change-set(JSONL)" 하나

경우의 수(DB/git × 폐쇄망/로컬 × 여러 API)가 많아 보이지만 **세 축이 독립**이라 실제 분기는 적다.

```
[생산: DMZ or 로컬]                 [전송]                    [소비: 내부 or 로컬]
API수집→정제→Diff→텍스트추출  ─▶  change-set 파일(JSONL)  ─▶  임베딩(온프레 arctic)→Upsert/Delete
  (git|db = 상태추적 방식)        (파일 단방향 | 직접)          (← 여기 코드는 항상 동일)
```

- **change-set(JSONL) = 유일한 경계 계약.** 한 줄 = 한 변경:
  ```json
  {"op":"upsert","source":"law","doc_id":"<law_id>","unit_id":"<provision_id>","text":"...","metadata":{...},"file_refs":["minio://..."]}
  {"op":"delete","source":"law","doc_id":"<law_id>"}
  ```
- **텍스트는 DMZ에서 이미 추출**(HWP/PDF→text)돼서 내부는 파싱 안 함, 임베딩만.
- **벡터는 내부에서** 생성(온프레 모델). DMZ는 벡터 안 만듦(텍스트+메타까지만).

## 1. 세 축이 무엇을 바꾸는가 (경우의 수 정리)

| 축 | 선택지 | **무엇이 달라지나** | 영향 계층 |
|---|---|---|---|
| **상태추적** | **git** / **db** | "무엇이 바뀌었나"를 기억하는 방식. git=`git diff`, db=`updated_at/content_hash 조회`. **둘 다 같은 change-set 생성** | 생산만 |
| **전송** | **직접** / **파일 단방향(MCCUBE)** | change-set을 넘기는 방법. 로컬=직접호출/로컬디렉토리, 폐쇄망=파일 drop→수신에이전트→Kafka | 전송만 |
| **초기/증분** | **초기=벌크** / **이후=change-set** | 처음은 전량 upsert(또는 Weaviate 백업 반입), 이후는 변경분만 | 데이터량만 |

→ **소비(임베딩+Upsert)는 어느 조합이든 불변.** git/db·폐쇄망 여부를 나중에 갈아끼워도 내부 코드 안 바뀜.
→ **"git 되면 상관없다"가 맞음**: git 되면 git 모드+`git bundle`을 전송파일로 쓰면 끝. db 모드는 DMZ에서 git 불가일 때의 대체(상태를 DB로, change-set은 DB 조회로 생성).

## 2. 계층별 책임

**① 생산 (DMZ / 로컬)**
- API 수집(4종) → 정규화(JSON/XML/HWP/PDF → text+metadata 통일).
- **HWP/PDF 텍스트 추출 = 여기서**(전처리기 `doc-parser` 이미지가 DMZ에 위치). 내부는 원본 파싱 안 함.
- Diff: 직전 상태 대비 변경 추출. 상태추적 = **git|db 플러그**.
- 출력: `change-set.jsonl` (+ 필요 시 원본 파일 별도).
- **4개 API는 모두 같은 change-set 계약으로 정규화**해 내보냄(source 필드로 구분). 소비자는 소스 안 가림.

**② 전송 (토폴로지 플러그)**
- 로컬/비폐쇄망: 생산자가 소비자를 **직접 호출**(또는 로컬 디렉토리 drop).
- 폐쇄망: `change-set.jsonl` 파일을 **MCCUBE 단방향** 반입(악성코드·CDR·화이트리스트·해시는 MCCUBE가 처리 — 우리는 "파일 쓰고/읽기"만 신경). 내부 수신에이전트가 Kafka에 적재.
- **우리가 만드는 건 "파일 생산/소비"뿐. MCCUBE 내부는 신경 안 씀.**

**③ 소비 (내부 / 로컬) — 불변 코어**
- change-set 읽기(디렉토리 | Kafka | 직접) → **온프레 임베딩(arctic)** → op대로 **Weaviate Upsert/Delete**.
- upsert/delete는 **doc_id(law_id) 단위 삭제 후 재적재**(개정 시 version_uid 변경으로 옛 청크 orphan 방지 — 기존 결정 그대로).

## 3. 파일(HWP/PDF/별표) 처리

- **RAG용**: DMZ에서 텍스트 추출 → change-set의 `text`로 들어감. **바이너리는 경계 안 넘어도 됨**(최소 전송).
- **원본을 내부에서도 제공해야 하면**: 바이너리를 MCCUBE(CDR)로 내부 **minio**에 반입 → change-set `file_refs`에 **내부 minio URL**만 실음. (DB 모드에서 파일→minio URL 얘기한 게 이거.)
- 즉 텍스트는 항상, 바이너리는 "내부 다운로드 필요 시에만".

## 4. 로컬 모드 (폐쇄망 아님)

세 계층을 **한 프로세스/네트워크로 접음**:
- 수집→Diff→change-set→임베딩→Upsert 를 그대로, 전송만 "직접"으로.
- Temporal 한 판에 얹으면 `collect → (change-set) → 임베딩 activity`로 자동. (내부용은 소비 코어를 activity로 감싸기만.)
- **같은 코드**가 로컬에선 접혀서, 폐쇄망에선 DMZ/내부로 갈라져 돎.

## 5. 권장 & 지금 결정

- **change-set(JSONL) 계약을 표준으로 확정** → 생산(git/db)·전송(직접/파일)·초기(벌크) 다 그 위에 얇게.
- **소비 코어(임베딩+Upsert)를 먼저 CLI 잡으로** 구현(로컬=직접, 폐쇄망=파일 읽기 동일). 내부 Temporal은 나중 activity 래핑.
- **상태추적 git/db는 생산자 플러그로 뒤로 미룸** — DMZ에서 git 가능 여부 확정 후 결정해도 내부 무영향. (git 되면 git 우선, 안 되면 db.)
- **전처리기(doc-parser)는 DMZ 소속**(텍스트 추출), 내부 임베딩기는 파일 파싱 안 함.
- **초기 이전**: 전량 change-set 또는 Weaviate 백업 반입(둘 다 가능, 백업이 빠름).

## 6. 미결(폐쇄망 확정 시)
- DMZ git 허용 여부 → 생산자 git|db 확정.
- 원본 바이너리 내부 제공 필요 여부 → minio 반입 여부.
- 판례/복지로/서민금융 API 스키마 → 각 정규화기(생산자) 추가(같은 change-set로).
- 내부 Kafka↔소비자 연결(수신에이전트 포맷) — 로컬은 디렉토리로 대체 가능.
