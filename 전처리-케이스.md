# 전처리 지시서 (법령류 + 행정규칙류)

> **어떤 파일을 전처리기(doc_parser attachment `/run`)로 돌리고, 무엇을 임베딩 텍스트로 쓰는지**만 정한다.
> (적재·change-set·임베딩 연결은 별도.) 실측 근거 2026-07-29.

## 0. 원칙

> **텍스트(`content`)가 payload에 있으면 그대로 쓴다. 실질 내용이 "이미지/파일에만" 있으면 그것만 전처리기로 텍스트를 뽑는다.**
> 파일을 고를 땐 **결정적(확장자·파일명 중복)** 기준만 쓰고, 파일명 키워드로 role 을 추측하지 않는다.

payload는 **조(article) 단위**. 조 → 별표 → 원문 순.

---

## 1. STEP별 처리

### STEP 1 — 조(article)
> 조문으로 안 나뉜 문서는 **`article_no="전문"` 인 조문 1개**로 온다(조문1 아님). 아래 규칙 그대로.
- `content` 그대로 사용.
- content에 **`[그림]`** 이 있으면 → 그 자리에 텍스트 대신 이미지가 온 것 → **본문이미지 파일 OCR** 로 `[그림]` 자리를 채운다.
  - `[그림]` 개수 == 본문이미지 개수(실측 완전 일치) → **순서대로** i번째 `[그림]` ↔ i번째 이미지.
  - ⭐ **이미지 마커는 `[그림]` 딱 하나뿐.** 그 외 모든 대괄호 표기 — `[별표N]`·`[별지]`·`[서식]`·`[별첨]`·`[붙임]`·`[시행일]`·`[전문개정]`·`[주]`·`[참고]` 등 — 은 **텍스트(참조·각주·표기)** 이므로 **그대로 둔다.**
  - 전문 content가 `[그림]` 이면 = 본문 통째가 이미지 → OCR (행정 3,275건).

### STEP 2 — 별표/별지(appendix)
- **삭제/이동 스텁 별표** — `content`이 `삭제`/`<삭제>` 또는 `…(으)로 이동 <날짜>` 표기(실내용 없음) → **임베딩 skip**(노이즈). 법령 304·행정 2,239+.
- `content` 있음 (`is_file_only=false`) → **그 텍스트 그대로. 파일 전처리 안 함.**
  - 단 별표 content에도 `[그림]` 있으면(드묾: 법령 2·행정 5) → 조와 동일하게 이미지 OCR 병합.
- `content` 없음 / `is_file_only=true` (파일밖에 없음) → **그 별표 파일을 §2 공통 규칙대로 전처리.**
  - (실측: content 없으면 항상 `is_file_only=true`. 삭제 별표만 빼면 "content 있음" ↔ "파일만" 으로 깔끔 분기)

### STEP 3 — 원문(attachments) = **본문 자체가 파일인 경우**
- 조문이 **있으면** → 원문은 **참고용 → 전처리 안 함** (내용이 조문/별표에 이미 있음). 진짜 "추가 첨부"가 섞여 있어도 **원문 vs 추가첨부를 가릴 결정적 신호가 없어 전부 skip**(fuzzy 판정 금지).
- 조문이 **없으면**(조문0 = 본문이 파일뿐) → **원문 폴더의 파일들을 §2 공통 규칙대로 전처리.**
  - 원문 안에 **원문(고시)·제개정이유서·신구조문 등이 섞여** 있어도 **role 구분 안 함**(결정적 신호 없음). → 공통 규칙(중복 제거 + 미지원 skip)만 적용하고 **남은 파일 전부** 전처리.
  - **예) 태안 고시(조문0)**: `조문별제개정이유서.hwpx`+`.pdf`(중복→1개) + `환경부고시제2023-206호.pdf` → 중복만 제거, **2개 파일 다 전처리.**
  - 조문0인데 원문(attachments) 없고 별표만 있으면 → STEP2로 처리(행정 7건).

### STEP 4 — 부칙(addenda)
- `content` 그대로. **전처리·OCR 안 함.**
- 부칙 끝의 `[그림]`(법령 650·행정 535)은 **실제 이미지 파일이 없음**(본문이미지 폴더에 부칙 이미지 미수집 — 공포 도장/직인 추정) → **OCR 대상 아님, 무시**(마커는 그대로/제거).

---

## 2. ★ 파일 전처리 공통 규칙 (STEP2·STEP3 공통, 결정적)

파일을 전처리기에 넣기 전, **딱 아래 3가지만** 결정적으로 적용한다. 파일명으로 role(원문/이유서 등) 추측 **금지**.

1. **미지원 확장자 skip** — `zip` · `hml` 은 전처리기가 못 읽음 → 제외.
   - 지원: `pdf · hwp · hwpx · docx · ppt/pptx · csv/xlsx · jpg/png/gif · txt`
   - 확장자는 **로컬 파일 실제 확장자** 기준(payload 파일명엔 확장자 없을 수 있음 — admrul).
2. **중복 포맷 제거** — 같은 파일명(basename)에 확장자만 다른 것(예 `X.hwp` + `X.pdf`) → **1개만**. 우선순위 `hwpx > hwp > docx > pdf`. (대소문자 무시)
3. **남은 파일 전부 전처리** → 청크. role/이름 기반 필터 안 함.

> 실측: 조문0+원문 778문서 중 **345문서가 포맷 중복**(hwpx+pdf 등) → 규칙2로 반감. `zip 610·hml 3` 은 규칙1로 제외.
> ⚠️ **전처리기는 입력 파일 옆에 중간 변환물(hwp→pdf 등)을 쓴다** → 입력 경로가 **쓰기 가능**해야 함(`:ro` 면 에러). 운영 시 원본 미러에 쓰지 말고 **파일을 writable temp 로 복사 후** `/run` 호출. (실측 확인)

---

## 3. 전처리 대상 한눈에 (소스 3개 × 텍스트 유무)

| 파일 소스 | 텍스트 이미 있음 → **스킵** | 파일/이미지에만 → **전처리** |
|---|---|---|
| ① 조 본문이미지 | `[그림]` 없음 | **`[그림]` 있음** → 이미지 OCR → 조에 병합 |
| ② 별표 파일 | `content` 있음 | **`is_file_only`** → 파일 전처리(§2) |
| ③ 원문(attachments) | 조문 있음(참고용) | **조문0(본문=파일)** → 원문 전처리(§2) |

---

## 4. 파일 폴더 위치 (수집기 미러, 로컬)

```
{문서}/{종}/                       종 = (법령) 법률·시행령·시행규칙 / (행정) 문서종
  ├─ {문서}.json / .md
  ├─ 본문이미지/  제{N}조_{순번}.{gif|png|jpg}   ← STEP1 [그림] 이미지 (조번호_순번, 전문은 전문_순번)
  ├─ 별표/       별표{N}_{제목}.hwp             ← STEP2 별표 파일
  ├─ 별지/       별지{N}_… · 서식{N}_….hwp      ← STEP2 별지/서식 파일
  └─ 원문/       {제목}.{pdf|hwpx|hwp…}          ← STEP3 attachments 원문 (조문0면 내용)
```
- 파일은 **로컬 폴더에 이미 받아져 있음** → URL 다운로드 없이 **폴더를 돌며** 처리.
- 매칭:
  - 본문이미지 = `제{조번호}_{순번}` (전문은 `전문_순번`). 실측 6,602/6,603.
  - **별표 매칭 주의** — 파일명 앞 숫자는 **순번**일 수 있음(예 `서식1의4_[별지 제1호 서식]…`). 진짜 번호는 **파일명 안 `[별표/별지 제N호]`** 에 있으니 **그걸로 매칭**해야 정확(앞숫자로 하면 ~1.6% 실패). 실패분은 폴더 파일 best-effort 연결.

---

## 5. 전처리기(attachment_processor)는 뭘 해주나

`POST /run {"file_path":"…","params":{}}` → **텍스트 추출 + 청킹 다 해줌** → `List[GenOSVectorMeta]`(청크당 1개).

| GenOSVectorMeta | 의미 |
|---|---|
| `text` | 청크 텍스트 |
| `i_page` | 페이지 번호 |
| `i_chunk_on_doc` | 문서 내 청크 순번 |

- **표 → 구조화 텍스트, 본문 → 텍스트, 이미지 → OCR(kor·eng)** 알아서. **파일만 주면** 됨(포맷 분기 불필요).
- 청크 크기 `chunk_size`(기본 ≈문서 1청크; 정밀도 위해 2000~8000자 권장).

---

## 6. 데이터 팩트 (근거)

- **`[그림]` ⟺ 본문이미지 1:1** — 법령 767조 · 행정 8,578조. content가 `[그림]`뿐(텍스트0)인 조 존재.
- **부칙 끝 `[그림]`** — 법령 650·행정 535. 단 **본문이미지 폴더에 파일 없음**(부칙 이미지 미수집, 도장 추정) → OCR 대상 아님. **별표 content 내 `[그림]`** — 법령 2·행정 5(드묾).
- **본문이미지 파일명 = `{article_no}_{순번}`**(전문은 `전문_순번`) — 실측 매칭 6,602/6,603.
- **삭제된 별표/서식**(content=`삭제`/`<삭제>`) — 법령 304·행정 2,239. **삭제 조문은 0**(삭제 조는 아예 제거됨).
- **별표 kind 다양** — 별표·별지·서식 외 별도·부록·별첨·양식·붙임·부표·부속서·도식(파일 매칭 시 감안).
- **법령류 5,598문서**: 조문 205,054(빈 content 0), 별표 39,937(`is_file_only` **55**, 파일 hwp/pdf/gif), **attachments 0**, 조문0 **0**.
- **행정규칙류 35,007문서**: 조문0(본문=파일) **785**(원문有 778·별표만 7), attachments 보유 21,099, 별표 `is_file_only` ~8.9%. 조문 shape=법령 동일.
- body.format 전부 `articles`. is_file_only 별표에 zip/hml **0**(다 지원포맷).

---

## 7. 예외 케이스 (실측)

### ✅ 발생 안 함 (규칙이 다 커버)
- 진짜 빈 조(content·이미지 둘 다 없음) **0**
- `[그림]` 개수 ≠ 이미지 개수 **0** (여러 개여도 일치 → 순서 매핑 안전)
- 본문이미지 url/파일 빈 것 **0**
- 내용 0 문서(조문0인데 원문·별표 다 없음) **0**
- 빈 별표 **0** · content 없는데 `is_file_only=false` 인 별표 **0**

### 🚫 알려진 한계
- **조문有 문서의 attachments: "원문 재포장" vs "진짜 추가첨부" 구분 불가.** payload에 role 플래그 없음(수집기가 첨부 role 미표시 — law.go.kr API가 종류를 안 주는 듯).
  - **결정 = skip.** 대부분 원문 재포장이라 조문/별표와 중복 → 전처리하면 **같은 내용 이중 임베딩**(21,099문서, 비용·노이즈). 진짜 추가첨부는 드물어 손실 작음. 정확히 살리려면 ⓐ 수집기 role 태깅(권장) or ⓑ 첨부 텍스트 대조.
- **title-only 별표** — `[별표1] 국회기의 규격 및 모형` 처럼 content가 **제목만**(실제 내용은 이미지/모형)인데 `is_file_only=false`. → 제목만 임베딩(under-embed) 가능. 결정적 탐지 어려움(짧은 content ≠ 반드시 미완). 지금은 그대로 둠.
- **is_file_only 별표 파일 매칭 ~1.6% 실패** — 파일명 순번/번호 불일치(§4). 대괄호 번호 매칭 or 폴더 best-effort 로 개선.
- **관세율표류 is_file_only 별표 = 로컬 파일이 또 다른 안내 stub** — 실측: `관세율표.hwp` 를 `/run` 하면 실표가 아니라 `"…아래 주소 …grtFile/....xls"` 안내만 나옴. **실제 표는 로컬 미러에 없는 remote `.xls`** → 로컬 파일만으론 실내용 못 얻음. (그 xls URL 을 별도로 받아야; 현재 미수집)

### ⚠️ 있음 → 반영
| 케이스 | 수 | 처리 |
|---|---|---|
| 한 조에 `[그림]` 여러 개 | 법령 250·행정 2,384 | 순서대로 이미지 OCR (STEP1) |
| `[별표N]/[별지]/[서식]` 참조 마커 | 법령 34·행정 **10,873** | **OCR 아님**, 텍스트 참조 그대로 (STEP1) |
| **전문**(조문 미분할, `article_no="전문"`) | 법령 26·행정 **5,404** | STEP1 그대로 (그중 `[그림]` 3,275건은 OCR) |
| 부칙 끝 `[그림]` | 법령 650·행정 535 | **실제 이미지 파일 없음**(미수집·도장 추정) → **OCR 안 함, 무시** |
| **삭제된 별표/서식**(content=`삭제`) | 법령 304·행정 2,239 | 임베딩 skip |
| **별표 content 내 `[그림]`** | 법령 2·행정 5 | 그 별표 이미지 OCR 병합 |
| 별표 **kind 다양**(별도/부록/별첨/양식/붙임/부표/부속서/도식) | — | 폴더 별표/별지, 파일명 `kind+번호`로 매칭 |
| 원문에 원문·이유서·신구조문 **섞임** | 행정 다수 | **role 구분 안 함**, §2 공통 규칙(중복제거+skip) 후 전부 전처리 |
| 원문 **포맷 중복**(hwpx+pdf) | 행정 345문서 | §2 규칙2로 1개만 |
| `zip`(610)·`hml`(3) | 행정 | §2 규칙1로 skip |
| 긴 별표 content | — | 그대로 둠(하위 청킹 없음) |
