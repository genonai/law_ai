# 진행상황 — §6 package 계약 기반 "어디서든 색인" (코드 변경 정리)

> 목표: 수집기(temporal_law)와 임베딩기(law_embedding)가 **같은 망이든 DMZ/내부망으로 갈라지든** 동일하게 돌게 한다.
> 방법: 둘 사이를 **§6 JSONL package(단일 전달 계약)** 하나로 통일. 수집기 = package 생산, 임베딩 = package 소비.
> (이 문서는 **코드 변경분만**. agent(RAG)·배포 실측은 제외.)

```
수집기(temporal_law) ──▶ §6 JSONL package ──▶ 임베딩기(law_embedding) ──▶ Weaviate
   "생산자(emit)"          단일 계약(파일)          "소비자(consume)"
```

---

## 1. §6 package 계약 — 두 파이프라인이 맞춘 것

### 1-1. 계약 형태
- **한 JSONL 파일 = 한 package.** 별도 manifest 없이 각 줄의 `record_type` 으로 구분.
- 첫 줄 `package_header`(source·mode 등), 마지막 줄 `package_footer`(record_count 검증).
- 모든 데이터 레코드는 `law_id` 를 가짐 → 소비자가 **law_id 단위**로 처리(개정 = 옛 청크 삭제 후 재적재, 멱등).

### 1-2. record_type ↔ 소비자 동작 (계약 핵심)

| record_type | 생산자가 담는 의미 | 소비자 동작 |
|---|---|---|
| `package_header` | source(law/admrul)·mode·strategy | 컬렉션 결정 |
| `document` | 법령 JSON 본문(payload inline) | `map_law_data`→청킹→임베딩 (본문·부칙·텍스트 별표) |
| `normalized_chunk` | 생산자가 미리 정규화·청킹한 본문 | 그대로 임베딩(보조 경로) |
| `preprocessed_chunk` | DMZ 전처리기가 뽑은 첨부 텍스트 청크 | 바로 임베딩(FILE 청크) |
| `file` | 원본 첨부파일(짧은 `transfer_name`으로 전달) | **내부 전처리 ON이면** 전처리 후 임베딩 / OFF면 보류 |
| `pending_attachment` | 파일도 못 보내고 DMZ 전처리도 불가 | 본문만 색인, 첨부는 보류 기록 |
| `delete` | 폐지 문서 | `delete_by_law_id` |
| `package_footer` | record 수·해시 | 소비 수 대조(검증) |

### 1-3. "어떻게 서로 계약을 맞췄나"
- **record_type 문자열·필드를 양쪽 코드가 동일하게 공유.** 수집기 `pipeline/common/package.py` 와 임베딩 `law_indexer/package.py` 가 같은 상수(`document`/`file`/…)와 같은 payload 구조를 쓴다.
- **9개 망 케이스(subcase)를 이 계약 하나로 흡수**: 소비자는 record_type 을 전부 이해하고, "무엇이 오느냐"는 생산자(resolver)가 망 환경에 맞게 고른다. 소비자가 실제로 켜고 끄는 옵션은 **딱 2개**(내부 전처리 on/off, 원문 저장 on/off).
- **라운드트립으로 계약 일치 검증**: 수집기 `emit_package` → 임베딩 `consume_package` 로 실제 흘려보내 document / preprocessed_chunk / file / pending / delete 조합이 객체까지 맞게 나오는지 확인.

---

## 2. 수집기(temporal_law) 코드 변경 — commit `474d4c1` (develop)

| 파일 | 변경 | 내용 |
|---|---|---|
| `pipeline/common/package.py` | **신규** | §6 **생산자**. `emit_package`/`emit_manifest_package`(변경감지 diff 는 기존 `changeset.py` 재사용) · `iter_file_units`(파일 전용 별표/문서 열거) · **subcase resolver 3종** |
| `pipeline/common/payloads.py` | **신규** | **payload_getter 실배선**: `make_git_payload_getter`(데이터 레포 JSON 읽기) · `make_db_payload_getter`(`store.get_current_payload`, postgres 현행 payload). 그동안 타입만 있고 구현 없던 갭을 채움 |
| `tests/test_package.py` | **신규** | 생산자 12 테스트(record 조립 · resolver 3종 · git/db getter · manifest diff→package) |

**subcase resolver 3종**(망 환경별 첨부 전략):
- `hold_attachments_resolver` — 첨부 보류(`pending_attachment`) [폐쇄망]
- `file_transfer_resolver(stage_fn)` — 원본 파일 전달(`file`, 짧은 transfer_name) [내부 전처리]
- `dmz_preprocess_resolver(preprocess_fn)` — DMZ 전처리 청크(`preprocessed_chunk`) [DMZ 전처리]

→ 요지: **"현재 payload 를 git/db 어디서 읽든, 첨부를 어떤 전략으로 담든 → §6 package 로 뱉는다."**

---

## 3. 임베딩기(law_embedding) 코드 변경 — commit `0dba472`·`b99fb8d`·`c523293` (temporal_version)

### 3-1. §6 소비자 + 자동감지 (`0dba472`)
| 파일 | 변경 | 내용 |
|---|---|---|
| `src/law_indexer/package.py` | **신규** | §6 **소비자** `consume_package`. record_type 전부 처리 · **law_id 단위 버퍼링 후 flush**(옛 청크 삭제→재적재, 멱등) · `map_law_data`/`map_attachment_data` 재사용 · 내부 전처리(`file`)·원문 저장(`OriginalStore`) |
| `src/law_indexer/pipeline.py` | 수정 | `index_changeset` **자동감지**: 첫 줄이 `package_header` 면 §6 소비자, 아니면 1세대 change-set → **명령 하나로 통일** |
| `src/law_indexer/preprocess.py` | 수정 | `run_doc_parser` **공용 함수로 승격**(pipeline·package 공유, 중복 제거) |
| `src/law_indexer/config.py` | 수정 | 소비자 옵션: `PACKAGE_PREPROCESS_FILES`·`PACKAGE_FILE_INBOX_DIR`·`PACKAGE_STORE_ORIGINAL`·`PACKAGE_ORIGINAL_DIR` |
| `src/law_indexer/cli.py` | 수정 | `index-changeset --source` 를 선택으로(§6 는 header 로 자동 결정) |
| `src/law_indexer/weaviate_store.py` | 수정 | **Weaviate API 키 인증**(`WEAVIATE_API_KEY`) + `WEAVIATE_SECURE`. 비우면 익명(로컬 그대로) → genos 등 인증 Weaviate 접속 |
| `tests/test_package.py`·`test_weaviate_auth.py` | **신규** | 소비자 record_type별 + 인증 테스트 |
| `docs/…`·`.env.example` | 수정 | 운영/현황 문서 + 옵션 문서화 |

### 3-2. Temporal 워커 (`b99fb8d`)
| 파일 | 변경 | 내용 |
|---|---|---|
| `src/law_indexer/worker.py`·`worker_workflows.py` | **신규** | law_embedding 을 **genos temporal 에 얹는 워커**. activity 2개(초기 full 색인 / §6 package 소비) + 워크플로(결정적·sandbox 안전 분리) |
| `src/law_indexer/cli.py` | 수정 | `worker` 서브커맨드(`python -m law_indexer worker`) — 이미지 ENTRYPOINT 유지한 채 워커 기동 |
| `pyproject.toml` | 수정 | `temporalio` 의존성(워커 실행 시에만 import) |
| `tests/test_worker.py` | **신규** | 워크플로/activity 정의·등록 스모크 |

### 3-3. RemoteEmbedder (`c523293`)
| 파일 | 변경 | 내용 |
|---|---|---|
| `src/law_indexer/embedder.py` | 수정 | **`RemoteEmbedder`** — arctic 로컬 로드 대신 **OpenAI 호환 `/v1/embeddings`(genos 모델 서빙) 호출**. `make_embedder` 팩토리 |
| `src/law_indexer/config.py` | 수정 | `EMBEDDING_BACKEND=local\|remote` · `EMBEDDING_API_URL`·`EMBEDDING_API_KEY` |
| `src/law_indexer/cli.py`·`worker.py` | 수정 | 임베더 생성을 팩토리로 통일(local/remote 스위치) |
| `tests/test_remote_embedder.py` | **신규** | 요청형식·index정렬·인증헤더·차원·팩토리 테스트 |

→ 요지: **"§6 package 를 먹고 + 인증 Weaviate 에 쓰고 + genos temporal 워커로 돌고 + genos 임베딩 모델을 원격 호출"** 가능하게.

---

## 4. 커밋 / 브랜치 요약

| repo | 브랜치 | 커밋 |
|---|---|---|
| temporal_law | develop | `474d4c1` §6 생산자 + payload_getter + resolver (origin·personal·gitlab push) |
| law_embedding | temporal_version | `0dba472` §6 소비자+Weaviate인증 · `b99fb8d` Temporal 워커 · `c523293` RemoteEmbedder (origin push) |

- 테스트: law_embedding 114 passed · temporal_law 12 passed · 생산자↔소비자 라운드트립 4/4.
- 원칙: 커밋에 AI 트레일러 없음. 로컬 arctic·익명 Weaviate 는 기본값으로 유지(기존 동작 안 깨짐).
