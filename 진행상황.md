# 진행상황

## 1. 데이터 수집

### 행정규칙류

- 행정규칙류 수집 완료
- 저장소: <https://github.com/genonai/admrul_data>

### 법령류

- 법령류 수집 진행 중
- 저장소: <https://github.com/genonai/law_data>
- 현재 전체 수집 대상 중 일부가 아직 남아 있음

## 2. 임베딩

- 임베딩 모델은 `Snowflake/snowflake-arctic-embed-m-v2.0`(arctic) 계열을 사용
- Weaviate에 약 100개 정도 샘플 적재 후 검색 테스트 진행
- `law_embedding`은 입력 경로를 받으면 하위 폴더를 재귀 순회하면서 색인 대상 JSON을 찾는 구조
- 기본 입력은 `data` 루트 기준이며, 그 아래 `law_data`, `admrul_data`를 돌면서 법령/행정규칙 JSON을 탐색
- 본문 JSON은 조문, 부칙, 별표/별지 단위로 payload를 만들고, 각 단위를 임베딩 후 Weaviate에 적재
- 임베딩 대상 텍스트는 법령명, 법종, 조문번호, 제목, 본문 등을 합친 `search_text` 중심
- 현재 벡터 임베딩은 `content`/`search_text` 기반으로 진행하고, 메타데이터는 검색 필터링과 답변 근거 표시용으로 함께 저장
- 첨부파일, 서식, 별표 파일 등 파일류는 바로 임베딩하지 않고 전처리 단계를 거치도록 구조만 분리
- 전처리는 현재 목업 상태로, 파일 경로를 받아 상위 법령 JSON을 찾고 임시 청크를 만드는 흐름까지만 연결
- 실제 HWP/PDF/HWPX/OCR 파싱은 아직 미연결이며, 이후 전처리 담당자가 `preprocess_file`을 실제 추출 로직으로 교체하면 됨
- 이미 전처리된 청크 JSON이 있으면 `index-attachment-chunks` 명령으로 바로 임베딩/적재 가능

## 3. RAG / LLM

- LLM은 Groq API 기반으로 PoC 테스트 진행
- 기본 질의는 Weaviate 검색 결과를 바탕으로 답변 생성
- 질문이 연혁, 개정 이력, 과거 시점, 최근 변경 여부 등을 묻는 경우 Git history 조회 분기로 연결
- Git history는 검색된 조문 id에 직접 의존하지 않고, 검색 결과의 원본 JSON 파일 경로(`git_path`)를 기준으로 문서 변경 이력과 diff를 가져오는 방식으로 정리

## 4. Git History 테스트

질문:

> 10ㆍ29이태원참사 피해자 권리보장과 진상규명 및 재발방지를 위한 특별법 시행령이 최근에 바꼈다는데 뭐가 바꼈어

답변 요약:

- 최근 변경은 2026년 5월 11일부터 시행된 일부개정
- 주요 변경 내용은 제10조의2부터 제10조의4까지 신설
- 포상금 지급 대상, 지급 방법/절차, 포상금 환수 규정이 추가됨

타임라인:

- 2026-05-11: 일부개정 시행
- 2026-01-02: 타법개정 시행
- 2025-10-01: 타법개정 시행
- 2025-01-14: 제정 시행

주요 변경 내용:

- 제10조의2: 포상금 지급 대상자 신설
- 제10조의3: 포상금 지급 방법 및 절차 신설
- 제10조의4: 포상금 환수 규정 신설
- 포상금은 3천만원으로 규정
- 보상금과 포상금은 중복 지급할 수 없음

근거:

- 10ㆍ29이태원참사 피해자 권리보장과 진상규명 및 재발방지를 위한 특별법 시행령 일부개정령
- 대통령령 제36297호, 2026.5.6

## 5. 일반 질의 테스트

### 112 장난 신고

질문:

> 장난으로 112에 신고하면 처벌받나요?

답변 요약:

- 장난으로 112에 신고하면 처벌받을 수 있음
- 「112신고의 운영 및 처리에 관한 법률」 제4조 제2항은 위급상황 대응 목적 외의 112신고나 거짓 112신고를 금지
- 다만 현재 검색 결과만으로는 구체적인 처벌 조항까지 충분히 연결되지 않음

메모:

- 아직 행정규칙, 판례 등이 충분히 연결되지 않았고 relation depth를 타지 않아서 답변 근거가 얕게 나오는 것으로 보임

### 출생신고 기한

질문:

> 아기 낳았는데 출생신고 언제까지 해야 하나요? | 가족관계의 등록 등에 관한 법률

답변 요약:

- 출생신고는 출생 후 1개월 이내에 해야 함
- 근거: 「가족관계의 등록 등에 관한 법률」 제44조 제1항

### 등산 중 조난

질문:

> 등산하다 조난당하면 119가 구조하러 오나요?

답변 요약:

- 등산 중 조난 등 위급상황이면 119 구조 대상이 될 수 있음
- 「119구조ㆍ구급에 관한 법률」 제8조에 따라 119구조대는 위급상황에서 요구조자의 생명 등을 신속하고 안전하게 구조하는 업무를 수행
- 「119구조ㆍ구급에 관한 법률 시행령」 제5조에 따라 산악구조대 설치 근거도 확인됨
- 단순 등산 지원이 아니라 위급상황이 전제됨

### 가정폭력 고소

질문:

> 가정폭력을 당했는데 고소가 가능한가요? 고소는 어떻게 해야할까요?

답변 요약:

- 피해자 또는 법정대리인은 가정폭력행위자를 고소할 수 있음
- 근거: 「가정폭력범죄의 처벌 등에 관한 특례법」 제6조 제1항
- 고소할 법정대리인이나 친족이 없는 경우, 이해관계인의 신청에 따라 검사가 고소할 수 있는 사람을 지정할 수 있음
- 근거: 같은 법 제6조 제3항
- 실제 절차에서는 경찰서 또는 검찰청을 통한 고소, 증거 정리, 추가 상담 등이 필요할 수 있음

## 6. 다음 작업

- VM에 migration
- 전체 데이터 기준 재임베딩
- 첨부파일 전처리 파이프라인 연결
- relation depth 기반 검색 확장
- 행정규칙/판례/관계 법령까지 묶는 검색 전략 개선
- Git history diff 요약 품질 개선
